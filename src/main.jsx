import React from "react";
import ReactDOM from "react-dom/client";
import ChezAliceKiosk from "../chez_alice_kiosk.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ChezAliceKiosk />
  </React.StrictMode>
);
